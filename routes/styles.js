import { StyleSheet } from 'react-native'
// define your styles
const styles = StyleSheet.create({
  iconContainer: {
    backgroundColor: '#FF1A1A2B',
    opacity: 0.8,
    height: 50,
    width: 50,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 10,
  },
})

export default styles
